version: "2"

run:
  tests: true
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unconvert
    - unused
    - usestdlibvars

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
